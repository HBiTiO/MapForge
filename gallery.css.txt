
/* Galerie améliorée */
.modal-image {
  position: relative;
}

.gallery-arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 44px;
  height: 44px;
  border: 1px solid rgba(180, 120, 255, .65);
  border-radius: 50%;
  background: rgba(10, 7, 15, .78);
  color: #fff;
  font-size: 34px;
  line-height: 1;
  place-items: center;
  cursor: pointer;
  z-index: 5;
  backdrop-filter: blur(8px);
  transition: .18s ease;
}

.gallery-arrow:hover {
  background: rgba(91, 39, 145, .9);
  transform: translateY(-50%) scale(1.06);
}

.gallery-prev { left: 16px; }
.gallery-next { right: 16px; }

.modal-gallery {
  padding: 12px 16px 4px;
  display: flex;
  gap: 10px;
  overflow-x: auto;
  scrollbar-width: thin;
}

.gallery-thumb {
  flex: 0 0 92px;
  width: 92px;
  height: 64px;
  padding: 0;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 10px;
  overflow: hidden;
  background: #111;
  cursor: pointer;
  opacity: .62;
  transition: .18s ease;
}

.gallery-thumb:hover,
.gallery-thumb.active {
  opacity: 1;
  border-color: rgba(123, 61, 255, .95);
  box-shadow: 0 0 0 2px rgba(123, 61, 255, .16);
  transform: translateY(-1px);
}

.gallery-thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

@media(max-width:520px) {
  .gallery-arrow {
    width: 38px;
    height: 38px;
    font-size: 28px;
  }
  .gallery-prev { left: 10px; }
  .gallery-next { right: 10px; }
}
